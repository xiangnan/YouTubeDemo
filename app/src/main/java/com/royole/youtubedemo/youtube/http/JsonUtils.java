package com.royole.youtubedemo.youtube.http;

import org.json.JSONException;
import org.json.JSONObject;

import java.util.HashMap;
import java.util.Iterator;
import java.util.Map;

/**
 * Copyright (C) 2015, Royole Corporation all rights reserved.
 * Author  yogu
 * Since  2016/11/23
 */


public class JsonUtils {
    /*public static Map getValueByFirstKeys(String jsonString, String... keys) {
        String key;
        String value;
        Map<String, String> maps = new HashMap<>();
        int i = 0;
        try {
            JSONObject jsonObject = new JSONObject(jsonString);
            Iterator iterator = jsonObject.keys();
            while (i < keys.length && iterator.hasNext()) {
                key = (String) iterator.next();
                if(keys[i].equalsIgnoreCase(key)){
                    maps.put(key,)
                }
            }
        } catch (JSONException e) {
            e.printStackTrace();
        }
    }*/
}
